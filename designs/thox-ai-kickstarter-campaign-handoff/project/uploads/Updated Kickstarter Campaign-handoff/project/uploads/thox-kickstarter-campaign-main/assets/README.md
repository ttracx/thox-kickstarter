# Assets

Place the horizontal Thox.ai logo here as `THOX_ai_Logo_Horiz.png`.

If present, the renderer embeds it at the top of every generated document. If
absent, the renderer prints a warning and continues, so document generation
never depends on the asset being available.
